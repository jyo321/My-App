version: 0.2

env:
  variables:
    S3_BUCKET: test123-frontend
    CLOUDFRONT_DISTRIBUTION_ID: EGDBNBBOV3NTU

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Entering frontend folder"
      - cd frontend
      - npm install

  build:
    commands:
      - npm run build

  post_build:
    commands:
      - aws s3 sync build/ s3://$S3_BUCKET --delete
      - aws cloudfront create-invalidation \
          --distribution-id $CLOUDFRONT_DISTRIBUTION_ID \
          --paths "/*"